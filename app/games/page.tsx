import {Footer,Header,SaveButton} from "../components";import {games} from "../data";
export default function Games(){return <main><Header active="games"/><section className="archive-hero"><p className="eyebrow"><span/>GAME ARCHIVE</p><h1>Three defining eras</h1><p>Begin with a focused comparison set: the foundation, the branching chronicle, and the open-world experiment.</p></section><section className="archive-body"><div className="game-grid">{games.map(g=><article className="game-card" key={g.id}><span className="game-year">{g.year}</span><p>{g.era}</p><h2><a href={`/games/${g.id}`}>{g.title}</a></h2><p>{g.summary}</p><dl><div><dt>Defining idea</dt><dd>{g.distinction}</dd></div><div><dt>Platforms</dt><dd>{g.platforms.join(" · ")}</dd></div><div><dt>Seed officers</dt><dd>{g.officers.length}</dd></div></dl><div className="game-actions"><a href={`/games/${g.id}`}>Explore game →</a><SaveButton kind="games" id={g.id} label={g.short}/></div></article>)}</div></section><Footer/></main>}
