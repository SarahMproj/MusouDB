import {shuRoute,routeSource} from "../archive/dw8xl";import {SourceLinks} from "../archive/source-links";
import {battles} from "../data";import {Footer,Header} from "../components";
export default function Battles(){return <main><Header active="battles"/><section className="archive-hero"><p className="eyebrow"><span/>BATTLEFIELD INDEX</p><h1>Where the chronicle turns</h1><p>Recurring conflicts connected across games, factions, and the officers who fought them.</p></section><section className="archive-body"><section id="shu-route"><h2>Shu’s hypothetical route · DW8XL CE</h2><p>The base DW8 campaign is included in Complete Edition. These stars connect across several battles.</p><details className="spoiler-panel"><summary>Reveal the complete Shu branch checklist</summary><ol>{shuRoute.map(step=><li key={step.battleId}><a href={`/battles/${step.battleId}`}>{battles.find(b=>b.id===step.battleId)?.name}</a><p>{step.goal}</p><p>{step.reward}</p></li>)}</ol><SourceLinks sources={[routeSource]}/></details></section><div className="battle-timeline">{battles.map(b=><article key={b.id}><span>{b.year}</span><div><p>{b.factions.join(" · ")}</p><h2><a href={`/battles/${b.id}`}>{b.name} →</a></h2><p>{b.summary}</p><div className="tags">{b.games.map(g=><a href={`/games/${g}`} key={g}>{g.toUpperCase()}</a>)}</div></div></article>)}</div></section><Footer/></main>}
