import {weapons,officerById} from "../data";import {Footer,Header} from "../components";
export default function Weapons(){return <main><Header active="weapons"/><section className="archive-hero"><p className="eyebrow"><span/>WEAPON INDEX</p><h1>Movesets across eras</h1><p>A first structured view of signature weapons, wielders, and playstyle traditions.</p></section><section className="archive-body"><div className="weapon-grid">{weapons.map((w,i)=><article key={w.id}><span className="weapon-number">{String(i+1).padStart(2,"0")}</span><p>{w.style}</p><h2>{w.name}</h2><div>{w.wielders.map(id=>{const o=officerById(id);return o&&<a href={`/officers/${id}`} key={id}>{o.name} →</a>})}</div><small>{w.games.map(g=>g.toUpperCase()).join(" · ")}</small></article>)}</div></section><Footer/></main>}
